var Cache = require('./Cache');

/**
 *
 * @type {{cacheWithSize: Function}}
 */
module.exports = {
    cacheWithSize: cacheWithSize
};

/**
 *
 * @param size
 * @returns {Cache}
 */
function cacheWithSize(size) {
    'use strict';

    return new Cache(size);
}
